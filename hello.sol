pragma solidity 0.8.7;

contract HelloWorld{
    function hello(string memory) public pure returns(string memory ){
        return "Hello Wrold";
    }
}

// Types and Variables

// Js is untayped Language 
// solidty 
// int a = 1;  // singed integer (both +ve and -ve number)
// uint aa = 2 // unsigned integer (only positive only nr >= 0)
// string b = "hello"
// bool c = false
// a = "hello" "Not work coz we declare a as int  throw the error
// address  d = ethereum address
